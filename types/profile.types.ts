export type Profile = {
  id: string;
  displayName: string;
  imageUrl: string;
  lastActivity: Date;
};

export type ProfileRaw = {
  id: string;
  display_name: string;
  image_url: string;
  last_activity: Date;
};
